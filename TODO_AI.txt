AI_INSTRUCTION:

This file contains active tasks and user-reported issues. Treat this file as
high priority guidance, not as a rigid or mandatory script.

Guidelines for AI agents:

1. Read this file before starting work and use it to understand current priorities.
2. Do not blindly execute everything. Verify system state first.
3. If parts are outdated or already implemented, clean them up or ignore them.
4. Prefer fixing root causes over mechanical changes.
5. If you detect bugs not listed here, fix them and note them below.
6. Do not force architectural changes unless clearly necessary.
7. This to-do list is a guide to intent, not a strict contract.

---

COMPLETED ITEMS (1-13)

All items below were implemented across sessions on 2026-04-09.

 #  | Summary                                    | Execution note
----|--------------------------------------------|-------------------------------------------------
 1  | Instruments table: distinct columns        | 7-col table: Name, Status, Avg Return, Operator, Faculty, Location, Links
 2  | Dashboard quick-intake panel               | page_size=3, max_height=none, inline Assign+Accept
 3  | Instrument queue blocks (flex-wrap grid)    | Up to 9 cards, 5 samples each, overflow link
 4  | Queue page columns                         | Request, Instrument, Status, Requester, Time, File, Action (op-only)
 5  | Stats war room                             | All counters clickable, tiles link to detail
 6  | (duplicate of #1)                          | —
 7  | Hover back button                          | Fixed circle in left margin on sub-pages
 8  | Instrument detail: 3-block layout          | Machine + queue + control; "Create New Request" button
 9  | Instrument detail: left/right split        | Machine 1/3, queue+control 2/3
10  | No scroll panes                            | All paginated_pane max_height='none'
11  | Role-based crawl                           | See ROLE_VISIBILITY_MATRIX.md; 4 bugs fixed
12  | Random-walk stress crawl (300 steps)        | POST /requests/new 500 fixed; comment 403 was crawler artifact
13  | Centralize UI macros                        | stat_blob, chart_bar macros; V hardcodes replaced in instrument_detail

Bugs fixed:
  - Instruments page crash: sqlite3.Row.get() replaced with bracket access
  - Dashboard Assign/Accept hidden from non-operator roles (gated by can_operate_queue)
  - faculty_in_charge role added to ROLE_ACCESS_PRESETS + dropdown options
  - POST /requests/new: bare request.form["field"] → request.form.get() with validation
  - instrument_detail.html: 100+ hardcoded role strings → {{ V }}
  - stats.html: removed redundant V redefinition

---

ACTIVE — Phase 3: Full Simulation + Architecture Polish

Execution order: attack in waves, parallelize where possible.

WAVE 1: Full Demo Population
  14. Build populate_full_demo.py
      - 25 instruments across 5 categories (Spectroscopy, Microscopy, Chromatography, Thermal, Surface)
      - 15 faculty members (professor_approver + faculty_in_charge roles)
      - 10 operators (some handling multiple instruments)
      - ~40 requester accounts (internal + external)
      - Secretary Kondhalkar as finance_admin exercising budgeting rights
      - 500 sample requests spread over 6 months (Oct 2025 – Apr 2026)
      - Realistic lifecycle: submitted → approved → received → scheduled → completed
      - Grant-based event log: track funding sources per request
      - Finance pane data: amounts, payment statuses, receipt numbers
      - Cross-instrument user permissions (requesters assigned to submit to specific instruments)

WAVE 2: Admin/Settings Page Redesign
  15. Redesign sitemap.html → settings.html (Apple Settings philosophy)
      - Left sidebar: section selector buttons (Core, Operations, Reporting, Admin)
      - Right panel: forms, toggles, and info for selected section
      - Remove useless/dead links; keep only functional navigation
      - Each section shows relevant controls (not just links)
      - Toggle buttons for quick settings where applicable

WAVE 3: Navigation + Widget Improvements
  16. Instrument hover navigation pane
      - Under nav "Instruments" link, show dropdown/hover pane listing all accessible instruments
      - Click to jump directly to that instrument's detail page
      - Show instrument code + status dot for quick identification

  17. Universal dialog/input widget
      - Reusable search-type panel widget
      - Text box input + optional file attachment
      - Message routing selector (choose person, role, or channel)
      - Use in: request comments, instrument events, issue flagging
      - Future: internal messaging about issues across the system

  18. Instrument control panel improvement
      - One stats panel (current metrics)
      - One queue panel (slice of global queue)
      - One admin form panel (metadata editing)
      - One info panel (read-only instrument details)
      - One form control panel (customize request form for this instrument)
      - Apply same panel philosophy to other feature pages

WAVE 4: Calendar Integration
  19. Link instrument downtime to calendar
      - Downtime blocks appear on calendar view
      - Calendar filters show/hide downtime by instrument
      - Calendar events clickable → navigate to instrument detail
      - Instrument detail shows upcoming downtime from calendar
      - Cross-page calendar presence: dashboard shows next downtime, queue shows scheduling conflicts

WAVE 5: Final Template Centralization
  20. Apply chart_bar macro to dashboard weekly/monthly charts
  21. Audit all remaining templates for ad-hoc patterns
  22. Ensure all role visibility uses {{ V }} or proper subset variables
  23. Document all macros in _page_macros.html, _request_macros.html, _stream_macros.html

WAVE 6: Documentation Rewrite
  24. Rewrite README.md
      - Progress pane showing what's done vs. planned
      - Clear distinction: README = quickstart + progress; PROJECT.md = formal spec
      - Any AI agent or human reading README + PROJECT.md can rebuild the system from scratch
      - Formal project documentation: every route, every template, every macro, every role

  25. Update PROJECT.md
      - Add new macros (stat_blob, chart_bar) to file structure
      - Add new roles/presets documentation
      - Update changelog with all Phase 2-3 work
      - Update TODO section
      - Ensure schema section matches current DB

WAVE 7: Verification ✅ (completed 2026-04-10)
  26. ✅ Visibility audit: 171/171 pass, 0 fail (8 roles × 12 pages)
  27. ✅ 500-action populate crawl: 0 server errors, 0 exceptions
  28. ✅ Fixed sitemap TypeError (dict.items → group.entries)
  29. ✅ Updated test matrix: finance_admin full access

---

ARCHITECTURE_NOTES (from previous discussion — kept for reference):

Philosophy preserved:
- One queue, one request card as source of truth
- Sliced visibility per role
- LAN-first simplicity
- Blobs within blobs (tiles containing widgets)
- No scroll panes; paginated view panes only
- Apple UX / Johnny Ive design sensibility

data-vis decision: Keep data-vis as defense-in-depth. Server-side rendering is primary
gate; client-side data-vis JS is secondary safety net. V includes all roles for elements
that are visible to everyone; use subset variables (VA, etc.) for restricted elements.

Component system: Enforce macros over ad-hoc markup. All stat blobs, card headings,
paginated panes, status badges, and chart bars use shared macros.

===================================================================
PHASE 4: ARCHITECTURAL IMPROVEMENT PLAN
===================================================================

Full crawl audit performed 2026-04-10 across all templates, CSS,
app.py, and JS/static assets. Findings organized by Apple/Ive/Ferrari
design philosophy: every element earns its place.

Execution order: attack in waves, parallelize where possible.

WAVE G: Breathing Room & Visual Density (Jony Ive Spacing)
  30. Increase card padding (1rem → 1.5rem) and table cell padding
      (0.5rem → 1rem). Tables are the #1 visual density problem.
  31. Reduce table columns from 7 to 4-5 max on:
      - Queue: merge Request+Instrument, hide Files column
      - Instruments: move Avg Return + Location to detail view
      - Finance revenue: collapse Paid/Free into single column
  32. Add section-level spacing (2rem gap between major sections).
      Currently sections touch each other — cramped, not Apple.
  33. Empty state component: replace all "No data" strings with
      centered icon + message + action link macro.
  34. Action button clarity: rename ambiguous verbs:
      - "Accept" → "Accept Sample"
      - "Take Up" → "Schedule Job"
      - "Assign" → "Assign to Operator"

WAVE H: CSS Architecture Cleanup
  35. Consolidate 3 duplicate button definitions (lines 2450, 2462,
      5186) into single .btn base with BEM modifiers.
  36. Extract 50+ hardcoded colors to CSS variables:
      - .operation-status (#f4f6f7) → var(--panel)
      - .operation-dot colors → var(--tone-active), etc.
      - .nav-menu hover (#eef5fb) → var(--panel-hover)
      - Input borders (#cbd7e3) → var(--line)
  37. Remove 14 !important instances from Prism grid system.
      Rebuild with BEM: .prism-grid__button--active
  38. Standardize breakpoints to 3: 640px, 900px, 1280px.
      Currently 12 different breakpoints scattered.
  39. Add light theme input :focus state (currently invisible).
      Add :disabled state for buttons (opacity 0.5, cursor: not-allowed).
  40. Create shadow scale: --shadow-sm, --shadow-md, --shadow-lg.
      Currently 8+ hardcoded shadow values.
  41. Move DayPilot vendor CSS (lines 2935-3032) to separate file
      or remove entirely (DayPilot JS is unused).
  42. Add z-index scale: --z-dropdown: 30, --z-sticky: 40,
      --z-modal: 50, --z-tooltip: 60. Currently 4 scattered values.

WAVE I: App Architecture & Code Quality
  43. Add database indexes (HIGH IMPACT, LOW EFFORT):
      - sample_requests(status)
      - sample_requests(instrument_id, status)
      - approval_steps(sample_request_id, status)
      - request_attachments(request_id, is_active)
      - instrument_operators(instrument_id, user_id)
      - audit_logs(entity_type, entity_id, created_at)
  44. Extract permission check decorator to eliminate 12+ duplicated
      access control patterns across routes:
      @instrument_access_required("manage_or_operate")
  45. Create RequestQueryBuilder to centralize the 6 near-identical
      SELECT sr.*, i.name ... JOIN patterns.
  46. Cache assigned_instrument_ids() in Flask g context.
      Currently queries 3 tables per page load per user.
  47. Break up request_detail() (682 lines!) into action handler
      classes: ApproveStepAction, MarkReceivedAction, etc.
  48. Add request status state machine:
      TRANSITIONS = { "submitted": ["under_review", "rejected"], ... }
      Enforce valid transitions; reject invalid status changes.

WAVE J: Accessibility & PWA (Apple-Quality Polish)
  49. Add ARIA attributes:
      - role="navigation" on nav dropdowns
      - aria-label on all interactive elements
      - aria-hidden="true" on data-vis hidden elements
      - Focus trap on modal dialogs (Tab key containment)
  50. Add skip navigation link for screen readers.
  51. Add meta tags for Apple polish:
      - <meta name="theme-color"> (matches --accent)
      - <meta name="apple-mobile-web-app-capable" content="yes">
      - <link rel="apple-touch-icon"> (192px + 512px)
  52. Create manifest.json for PWA installability.
  53. Add toast notification system (bottom-right, auto-dismiss 3s)
      to replace flash messages that require scroll-to-top.
  54. Add client-side form validation with inline error states.
      Currently all validation is server-side with page reload.

WAVE K: Missing Production Features
  55. Per-instrument custom request form fields:
      - XRD needs crystal_system, ICP-MS needs sample_matrix
      - Admin UI to define fields per instrument
      - Dynamic form rendering on /requests/new
  56. Bulk operations on queue page:
      - Checkbox select multiple requests
      - Bulk approve, bulk assign, bulk download
  57. Notification system:
      - Approval request emails (approve by deadline)
      - Sample dropoff reminders (N days overdue)
      - Status change notifications
  58. Cost/grant tracking:
      - Grant/account code per request
      - Invoice generation
      - Payment reconciliation UI
  59. Approval chain customization UI:
      - Admin can reorder/skip approval steps per instrument
      - Default approver selection
  60. Remove demo mode from production:
      - Gate /demo/switch/ behind DEMO_MODE env flag
      - Don't seed demo users on init_db() in production
  61. Add Flask-WTF CSRF protection on all POST routes.

WAVE L: Documentation & API
  62. Create /api/v1 blueprint with JSON endpoints:
      - GET /api/v1/requests — list with filters
      - GET /api/v1/requests/:id — detail
      - GET /api/v1/instruments — list
      - POST /api/v1/requests/:id/actions/:action — state changes
  63. Add OpenAPI/Swagger documentation.
  64. Create .env.example with all required environment variables.
  65. Create config.py with environment-specific defaults
      (DevelopmentConfig, ProductionConfig).
