# CATALYST Cleanup Tasks — Generated 2026-04-13

> Auto-generated by full-codebase audit. Each section is a self-contained
> work package an agent can claim and ship in one commit. Sections are
> ordered by impact (crawler FAILs first, then WARNs, then improvements).
>
> **Codex claim active on:** `templates/attendance_team.html`,
> `templates/audit_log.html`, `templates/compute_detail.html`,
> `templates/compute_job_detail.html`, `templates/finance_grant_detail.html`,
> `templates/finance_grant_expenses.html`, `templates/finance_invoice_detail.html`,
> `templates/finance_invoice_form.html`, `templates/finance_spend.html`,
> `templates/letter_detail.html`, `templates/letter_form.html`,
> `templates/letters.html`, `templates/receipt_inbox.html`,
> `templates/todos.html`, `static/styles.css`, `CLAIMS.md`
>
> **Do not touch those files** until the Codex claim is released.

---

## T1. ~~Fix philosophy crawler FAILs (2 FAILs)~~ SHIPPED

**Goal:** Get philosophy crawler to 0 FAILs.

| Template | Issue | Fix |
|----------|-------|-----|
| `ca_audit_print_signoff.html` | Does not extend base.html, no tiles grid, no data-vis | Standalone print page — rename to `_ca_audit_print_signoff.html` (prefix `_`) and update the `render_template()` call in app.py |
| `filing_destroy_plan.html` | Does not extend base.html, no tiles grid, no data-vis | Standalone print page — rename to `_filing_destroy_plan.html` and update `render_template()` in app.py |

**How to find the render calls:**
```bash
grep -n "ca_audit_print_signoff\|filing_destroy_plan" app.py
```

**Verify:** `.venv/bin/python -m crawlers run philosophy` → 0 FAILs

---

## T2. Fix CSS orphan crawler FAIL (62 orphans, threshold 25)

**Goal:** Remove orphaned CSS selectors to get below the 25-orphan threshold.

**BLOCKED** — `static/styles.css` is claimed by Codex. Queue this for after
the claim is released.

Full orphan list (62 selectors to remove):

```
.approval-chain-approved, .approval-chain-pending, .approval-chain-rejected
.dev-timeline-link, .dev-timeline-tag
.dp-bridge-detail, .dp-todo-done, .dp-todo-item, .dp-todo-list, .dp-todo-mark
.finance-grant-head, .finance-grant-link, .finance-grant-list,
  .finance-grant-meta, .finance-grant-row, .finance-grant-title
.finance-invoice-paid-row
.finance-kpi-collected, .finance-kpi-link, .finance-kpi-outstanding
.finance-remaining-overdue, .finance-remaining-paid, .finance-remaining-pending
.finance-status-paid, .finance-status-partial, .finance-status-pending,
  .finance-status-void
.grant-bar-over, .grant-bar-warn
.grant-status-active, .grant-status-closed, .grant-status-completed,
  .grant-status-exhausted, .grant-status-frozen, .grant-status-suspended
.inline-self-center
.notice-clickable, .notice-head, .notice-row-header, .notice-row-meta,
  .notice-severity-critical, .notice-unread-dot
.notif-cat-badge, .notif-cat-label, .notif-cat-label-admin,
  .notif-cat-label-finance, .notif-cat-label-instrument,
  .notif-cat-label-request, .notif-cat-tab, .notif-cat-tab-active,
  .notif-cat-tabs
.notif-sev-critical, .notif-sev-info, .notif-sev-warning, .notif-severity-dot
.payment-dot-partial, .payment-dot-pending
.queue-position-value
.side-center, .side-left, .side-right
.tile-dev-now-shipping
```

**Verify:** `.venv/bin/python -m crawlers run css_orphan` → 0 FAILs

---

## T3. Fix philosophy WARNs — hardcoded colors (8 remaining templates)

**Goal:** Move inline/raw color literals to CSS classes. Needs new CSS
classes first (T3a), then template updates (T3b).

**T3a — Add badge tone classes to `styles.css`** (BLOCKED by Codex claim):
```css
.badge-tone-green  { background: var(--tone-green);  color: #fff; }
.badge-tone-amber  { background: var(--tone-amber);  color: #fff; }
.badge-tone-blue   { background: var(--tone-blue);   color: #fff; }
.badge-tone-red    { background: var(--tone-red);    color: #fff; }
/* + dark mode variants */
```

**T3b — Replace inline styles in 8 templates** (after T3a):

| Template | Lines | Pattern |
|----------|-------|---------|
| `filing_retention.html` | 31, 67, 92 | `style="border-left:3px solid var(--danger,#c44)"` etc |
| `payroll.html` | 70, 72, 79, 81 | `style="color:var(--tone-amber, #f59e0b)"` etc |
| `personnel.html` | 61, 63, 65 | `style="background:var(--tone-X);color:#fff"` → class |
| `personnel_detail.html` | 139, 141 | Same badge pattern |
| `tally_export.html` | 28, 32, 36, 40, 115 | `style="background:var(--bg-secondary,#f5f5f5)"` |
| `vehicle_detail.html` | 132, 134, 136 | Badge tone pattern |
| `vehicles.html` | 62, 64, 66 | Status badge pattern |
| `attendance_team.html` | (Codex-claimed) | Skip until released |

**Verify:** `.venv/bin/python -m crawlers run philosophy` → 0 WARNs

---

## T4. CSS duplicate/conflicting selectors

**BLOCKED** — `static/styles.css` claimed by Codex.

| Selector | Lines | Issue |
|----------|-------|-------|
| `.status-completed` | ~1783 vs ~2534 | Second definition uses hardcoded colors instead of CSS vars |
| `.status-in_progress` | ~1778 vs ~2524 | Same issue |
| `.approval-action-block` | ~2576 vs ~4536 | Completely different properties |
| `.approval-action-heading` | ~2582 vs ~4545 | Font size conflicts |
| `.tile-maintenance` | ~3704 vs ~8256 | `span 3` vs `span 2` |

**Fix:** Remove the later conflicting definitions, keep the one using CSS vars.

---

## T5. Template CSRF hardening

**Goal:** Add explicit `csrf_token` to macro-generated forms.

| File | Line | Issue |
|------|------|-------|
| `_page_macros.html` | ~84 | `input_dialog()` POST form missing csrf_token |
| `_page_macros.html` | ~306 | `queue_action_stack()` form 1 missing csrf_token |
| `_page_macros.html` | ~314 | `queue_action_stack()` form 2 missing csrf_token |

These currently rely on the base.html JS auto-injection shim, which is fragile.
Add `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">` to each.

---

## T6. Hardcoded URLs in templates and JS

| File | Line | Current | Should be |
|------|------|---------|-----------|
| `_hub.html` | ~61 | `href="/"` | `href="{{ url_for('index') }}"` |
| `_hub.html` | ~79 | `href="/admin/portfolio"` | `href="{{ url_for('portfolio_panel') }}"` |
| `user_detail.html` | ~4 | `javascript:history.back()` | `{{ request.referrer or url_for('index') }}` |
| `static/keybinds.js` | ~25 | `'/requests/new'` | Use `data-` attribute from template |

---

## T7. JS toggle code deduplication

**Files:** `intake-toggle.js`, `approval-toggle.js`, `role-toggle.js`

All three implement identical "2-tap arming with 3-second disarm" pattern.
Extract shared logic into `static/two-tap-toggle.js` utility:
- `armed` / `armedTimer` state
- `disarm()` function
- `message()` helper
- Click handler wrapper

Estimated reduction: ~200 lines of duplicated code.

---

## T8. Security improvements (non-breaking)

| Priority | Issue | Location | Fix |
|----------|-------|----------|-----|
| HIGH | CSRF defaults to OFF | app.py ~107 | Change default: `os.environ.get("LAB_SCHEDULER_CSRF", "1")` |
| HIGH | No rate limiting on /login | app.py ~9788 | Add Flask-Limiter (5/min on /login, /activate) |
| MEDIUM | /hub missing @login_required | app.py ~6977 | Add decorator or confirm intentionally public |
| MEDIUM | No file size validation on uploads | app.py ~1565 | Add explicit max size check |
| LOW | Hardcoded IP in backup script | scripts/backup_from_mini.sh ~27 | Require env var, remove default IP |

---

## T9. Architecture WARNs (14 from crawler)

Run `.venv/bin/python -m crawlers run architecture` with verbose output
to get the 14 WARN details. Likely handler/template size warnings.
Fix by extracting large handlers into helper functions.

---

## T10. LaunchAgent TCC fix (laptop)

The laptop LaunchAgent (`local.catalyst`) cannot start because macOS TCC
blocks background processes from accessing `~/Documents/`. The gunicorn
binary and Python venv are under `~/Documents/Scheduler/Main/`.

**Options (pick one):**
1. System Settings → Privacy & Security → Full Disk Access → add `/bin/bash`
2. Move the working copy to `~/Scheduler/Main/` (non-TCC path)
3. Create a venv symlink outside Documents (won't work — TCC checks real paths)

**Current workaround:** gunicorn runs as a manual daemon (works, but no auto-restart).
